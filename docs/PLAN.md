# 当前计划

## 正在做

- [ ] [Harmony/语音基础] 完成当前音频闭环的真机验收与回归
  - [ ] 验证已连接耳机时的麦克风路由与图标状态
  - [ ] 验证听筒 / 扬声器切换，以及无耳机、仅输出耳机、耳机热插拔场景
  - [ ] 验证 `audioSessionDeactivated` 后的有界恢复
  - [ ] 麦克风开启和关闭两种状态下验证自动 TTS 可听、确认 `6800301` 不再出现
  - [ ] 在真实手机上确认 ASR、TTS、Agent 过程状态和终止状态一致

## 下一步

### 1. 动态 Agent 与语音回复策略

- [ ] [Agent与语音] 落实动态 Agent 目录、顶部身份展示和语音回复策略
  - [ ] 定义并测试 Agent Catalog 接口及后端配置结构，客户端不写死 Profile
  - [ ] HarmonyOS 改为动态 Agent 状态，处理加载、空列表、删除回退和切换
  - [ ] 重构顶部 Agent 区域，加入头像占位及当前模型 / Provider
  - [ ] 接入后端语音回复策略，不污染用户消息和历史记录
  - [ ] 增加 `speech_text` 兼容与 TTS Markdown / 代码块过滤
  - [ ] 完成静态检查、自动化测试、HAP 构建和真机验收
  - [ ] 详细计划：`docs/plans/2026-08-10-dynamic-agents-and-voice-response.md`

### 2. 思考过程按行展示

- [ ] [消息展示] 将 `thinking.delta` / `reasoning.delta` 按自然行拆分展示，不再把整段思考揉成一个长段落
  - [ ] 保留服务端换行和阶段顺序，连续增量只追加到对应行
  - [ ] 处理没有换行的增量，避免每个 token 都产生一行
  - [ ] Web 与 HarmonyOS 保持一致，过程区仍可折叠、滚动和显示完成状态
  - [ ] 验收：长思考过程能逐行阅读，正文、工具过程、思考过程不混在一起

### 3. Hermes Agent 设置增加模型 / Provider 快速切换

- [ ] [Hermes 设置] 增加可视化的模型和供应商选择入口，避免手动修改配置文件
  - [ ] 从 Hermes 官方设置能力或受控接口读取可用 Provider / 模型列表，不在客户端硬编码密钥
  - [ ] 明确“只切换当前会话”与“写入当前 Profile 默认配置”的边界
  - [ ] 切换过程中保留当前对话和语音连接的状态，失败时显示原因并保持原配置
  - [ ] Web 与 HarmonyOS 的入口和状态反馈保持一致
  - [ ] 验收：选择后能确认实际生效的模型、Provider，重连或恢复 Session 后状态不漂移

### 4. 单条消息重读 / 播放

- [ ] [消息播报] 在适合的位置为可播报消息增加重读按钮
  - [ ] 只触发该条消息的 TTS，不新建 Hermes 对话、不重复执行 Agent
  - [ ] 支持重新播放、播放中停止或排队，遵守当前语音输出 / 静音状态
  - [ ] 正确处理长文本、代码、文件和 `MEDIA:`：屏幕完整保留，TTS 使用清理后的可播报文本
  - [ ] Web 与 HarmonyOS 分别适配桌面、移动端和原生触控布局
  - [ ] 验收：不看文字时可重新听完指定消息，播放状态不会污染当前 Agent 任务

### 5. 延长语音输入停顿判定

- [ ] [连续 ASR] 延长气口 / 短暂停顿的收句判定时间，避免用户思考时被系统抢话
  - [ ] 先梳理 Web、HarmonyOS、豆包流式 ASR 各自的结束判定和缓冲参数
  - [ ] 将短暂停顿、真正结束、长时间无声区分为独立状态
  - [ ] 保留用户主动提交、停止和闭麦的优先级，不让延长窗口阻塞明确指令
  - [ ] 使用真实手机外放 / 耳机分别验证连续表达、思考停顿和真正结束
  - [ ] 验收：短暂停顿不会过早提交；真正结束仍能在可接受延迟内提交

### 6. 语音输入纯符号前置过滤

- [ ] [ASR 输入] 在 ASR final 进入 Hermes 前增加纯标点 / 符号过滤
  - [ ] trim 后判断是否只包含句号、逗号及其他 Unicode 标点、符号或空白
  - [ ] 纯符号结果不创建正式用户消息、不进入队列、不触发 Agent 回复
  - [ ] 保留包含汉字、字母、数字或实际语义内容的正常短句
  - [ ] 明确过滤发生在 Web 与 HarmonyOS 的共同边界，避免两端行为不一致
  - [ ] 验收：纯 `。`、`,`、`…`、重复标点等被丢弃；正常短句和带标点短句正常提交

### 7. “闭麦”指令与停止任务分离

- [ ] [语音控制] 增加“闭麦”控制指令，关闭麦克风输入但不影响 Agent 思考和 TTS 播报
  - [ ] “停止 / stop”继续只负责中断当前 Agent 任务并清理 TTS 队列
  - [ ] “闭麦”只停止 `AudioCapturer` / ASR 输入，保持 Hermes Gateway、当前任务和播报连接
  - [ ] 闭麦指令本身作为控制词消费，不进入对话历史、不触发新的 Agent 回复
  - [ ] 提供明确的重新开麦方式，并显示输入关闭、输出仍可用的状态
  - [ ] 处理闭麦期间到达的 Agent 结果：仍可播报；不因输入关闭而丢弃
  - [ ] 验收：真实手机上说“闭麦”后麦克风确实停止，当前思考和播报不中断；说“停止”仍能中断任务

## 待决策

- 模型 / Provider 切换是仅影响新 Session，还是允许当前 Session 平滑切换；默认配置是否允许写回当前 Hermes Profile。
- 思考过程的“一行”按服务端换行、语义阶段，还是客户端自然分段；需要避免 token 级碎片化。
- 重读按钮默认只显示在 Agent 消息，还是用户消息也提供“朗读转写”；多条重读请求采用排队还是替换。
- 气口延长的具体阈值、最大等待时间和 Web / HarmonyOS 是否共用同一组参数。
- 纯符号过滤的 Unicode 分类范围，以及“闭麦”识别的精确词表和误触发处理。

## 关联现有文档

- 当前产品与架构：`docs/ARCHITECTURE.md`、`docs/PRODUCT_FORMS.md`
- 当前交互协议：`docs/INTERACTION_AND_PORTING_SPEC.md`
- 语音输入输出边界：`docs/plans/2026-08-10-voice-input-output.md`
- 当前 HarmonyOS 状态与真机阻塞：`docs/STATUS.md`
- 动态 Agent 与语音回复：`docs/plans/2026-08-10-dynamic-agents-and-voice-response.md`
